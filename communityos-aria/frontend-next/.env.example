# ARIA Chat API base URL. Defaults are the deployed Render URLs.
# Frontend reads these only as defaults — the user can override in the
# sidebar Configuration card at runtime.
NEXT_PUBLIC_ARIA_BASE_URL=http://localhost:8080
NEXT_PUBLIC_T2T_BASE_URL=http://localhost:8000
NEXT_PUBLIC_DEFAULT_TWIN_ID=tanmay_resident
NEXT_PUBLIC_DEFAULT_ORG_ID=maple_heights
NEXT_PUBLIC_DEFAULT_API_KEY=tanmay-key-001
